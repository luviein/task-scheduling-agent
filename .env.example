GEMINI_API_KEY=your-key-here
# Optional: override the default model
# GEMINI_MODEL=gemini-2.5-flash

# mock (default) or google. google needs credentials.json in the project root.
CALENDAR_BACKEND=mock
